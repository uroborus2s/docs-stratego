# 需求变更日志

记录需求级别的变化、审批结果和关联的变更请求。

## 2026-04-02

- 已批准 `CR-002`：重构接入文档，把公开标准、接入、联动、移除与 CLI 命令收口到 `Contributor Guide`。
- 已批准 `CR-003`：新增源仓接入/移除辅助 CLI，覆盖 `source validate`、`source add`、`source scaffold-notify`、`source remove`。
